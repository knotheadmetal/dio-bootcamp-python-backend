frutas = (
    "laranja",
    "pera",
    "uva",
)
print(frutas)

letras = tuple("python") # criando uma tupla a partir de uma string
print(letras)

numeros = tuple([1, 2, 3, 4]) # criando uma tupla a partir de uma lista
print(numeros)

pais = ("Brasil",) # criando uma tupla com um único elemento
print(pais)
